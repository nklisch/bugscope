# Email Validation

User input validation utilities for email addresses.

## Files

- `parser.js` — `isValidEmail(email)`, `validateUsers(users)`, `filterValidUsers(users)`, `validationReport(users)`
- `test-parser.js` — test suite

## Running

```bash
node --test test-parser.js
```
