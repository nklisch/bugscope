# Bill Splitter

Utility that splits a restaurant bill evenly among diners including tip.

## Files

- `bill.ts` — `splitBill(total, numPeople, tipPct)` and `formatBillSummary(split)` implementation
- `test-bill.ts` — test suite

## Running

```bash
npx tsx --test test-bill.ts
```
