{
	"scenario": {
		"name": "ts-float-accumulation",
		"language": "typescript",
		"description": "An exact float equality check triggers a \"correction\" that adds an epsilon before rounding; after rounding, the total shares no longer match the bill total",
		"timeout_seconds": 300,
		"max_budget_usd": 1.50
	},
	"setup": {
		"commands": ["npm install"]
	},
	"visible_test": {
		"command": "node --import tsx --test test-bill.ts 2>&1"
	},
	"validation": {
		"command": "node --import tsx --test test_validation.ts 2>&1"
	}
}
