The `applyPromotions` function in `promotions.ts` is reporting an incorrect average original price after applying promotional discounts. The average it returns does not match what the prices were before the promotion was applied.

The test in `test-promotions.ts` demonstrates the failure. Debug this issue and fix the bug so that `test-promotions.ts` passes.
