{
	"scenario": {
		"name": "ts-runtime-registry",
		"language": "typescript",
		"description": "A dependency injection container uses hash-computed service keys; the RateLimiter declares a dependency on CacheService with the wrong variant string, producing a key that doesn't match any registered service",
		"timeout_seconds": 360,
		"max_budget_usd": 2.00
	},
	"setup": {
		"commands": ["npm install"]
	},
	"visible_test": {
		"command": "node --import tsx --test test-services.ts 2>&1"
	},
	"validation": {
		"command": "node --import tsx --test test_validation.ts 2>&1"
	}
}
