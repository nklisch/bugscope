{
	"scenario": {
		"name": "ts-mapped-type-pipeline",
		"language": "typescript",
		"description": "An analytics event pipeline conditionally normalizes purchase revenue from cents to dollars via schema transforms, but events without a matching schema pass through raw; aggregation sums mixed units",
		"timeout_seconds": 360,
		"max_budget_usd": 2.00
	},
	"setup": {
		"commands": ["npm install"]
	},
	"visible_test": {
		"command": "node --import tsx --test test-pipeline.ts 2>&1"
	},
	"validation": {
		"command": "node --import tsx --test test_validation.ts 2>&1"
	}
}
