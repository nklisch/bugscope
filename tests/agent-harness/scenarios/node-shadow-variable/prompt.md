The `processOrders` function in `orders.js` is returning an incorrect grand total. For a list of orders, the grand total is always higher than expected — as if an extra amount is being added in.

The test in `test-orders.js` demonstrates the failure. Debug this issue and fix the bug so that `test-orders.js` passes.
