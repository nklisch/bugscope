The `process_items` function in `process_items.py` is not processing all items. The test in `test_items.py` demonstrates the failure.

Debug this issue and fix the bug so that `test_items.py` passes.
