{
	"scenario": {
		"name": "ts-generic-constraint",
		"language": "typescript",
		"description": "A falsy truthiness check `if (config.version)` treats version 0 as \"not set\", but version 0 is a valid override meaning \"use legacy mode\", producing wrong configVersion and missing feature flags",
		"timeout_seconds": 300,
		"max_budget_usd": 1.50
	},
	"setup": {
		"commands": ["npm install"]
	},
	"visible_test": {
		"command": "node --import tsx --test test-service.ts 2>&1"
	},
	"validation": {
		"command": "node --import tsx --test test_validation.ts 2>&1"
	}
}
