The `processOrders` function in `orders.ts` is returning an incorrect grand total. When multiple orders are processed, the returned total is higher than expected.

The test in `test-orders.ts` demonstrates the failure. Debug this issue and fix the bug so that `test-orders.ts` passes.
