The `formatUserSummary` function in `user-loader.ts` is throwing a TypeError at runtime even though the TypeScript types look correct. Callers are getting a crash instead of a formatted user summary string.

The test in `test-user-loader.ts` demonstrates the failure. Debug this issue and fix the bug so that `test-user-loader.ts` passes.
