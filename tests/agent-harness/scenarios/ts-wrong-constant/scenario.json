{
	"scenario": {
		"name": "ts-wrong-constant",
		"language": "typescript",
		"description": "The gold tier discount is 1.0 (100%) instead of 0.1 (10%), giving gold customers a free order",
		"timeout_seconds": 120,
		"max_budget_usd": 0.50
	},
	"setup": {
		"commands": ["npm install"]
	},
	"visible_test": {
		"command": "node --import tsx --test test-pricing.ts 2>&1"
	},
	"validation": {
		"command": "node --import tsx --test test_validation.ts 2>&1"
	}
}
