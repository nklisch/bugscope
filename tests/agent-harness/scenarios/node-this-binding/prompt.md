The `computeInvoiceTax` function in `calculator.js` is returning `NaN` for all tax and total values. The tax rates are configured correctly and the inputs look valid, but every computed value comes back as `NaN`.

The test in `test-calculator.js` demonstrates the failure. Debug this issue and fix the bug so that `test-calculator.js` passes.
