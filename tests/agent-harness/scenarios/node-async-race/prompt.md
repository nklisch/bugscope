The `file-cache.js` module has a bug that causes reads to return stale data after writes. The test in `test-cache.js` demonstrates the failure.

Debug this issue and fix the bug so that `test-cache.js` passes.
